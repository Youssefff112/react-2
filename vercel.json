{
  "buildCommand": "cd recipe-app && npm install && npm run build",
  "outputDirectory": "recipe-app/dist",
  "installCommand": "cd recipe-app && npm install",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
